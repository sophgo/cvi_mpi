mw_dir=middleware
includedir=${mw_dir}/include
internal_includedir=${mw_dir}/internal_include
libdir=${mw_dir}/lib

Name: CVITEK System lib
Description: SYS / VI / VPSS/ VO / RGN / GDC / ISP / VENC / BIN libs
Version: 2.0
Cflags: -I${includedir} -I${internal_includedir} -I${includedir}/isp/cv181x
Libs: -L${libdir} -lmsg -lcvilink -lsys -lvi -lvpss -lvo -lrgn -lgdc -lvenc -lcvi_bin -lcvi_bin_isp -lisp -lae -laf -lawb
